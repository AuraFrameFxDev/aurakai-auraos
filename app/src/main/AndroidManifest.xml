<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- General foreground service permission -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" /> <!-- Specific data sync foreground service permission -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Optional Permissions - Declare runtime permissions for Android 6.0+ -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> <!-- Required for background location on Android 10+ -->

    <!-- Optional Features - Declare hardware requirements -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" /> <!-- Set microphone as optional if not strictly required -->
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" /> <!-- Optional: If GPS is not strictly required -->
    <uses-feature
        android:name="android.hardware.location.network"
        android:required="false" /> <!-- Optional: If network location is not strictly required -->

    <!-- Custom permission declaration -->
    <permission
        android:name="dev.aurakai.auraframefx.permission.RECEIVE_INTEGRITY_VIOLATION"
        android:protectionLevel="signature" />

    <application
        android:name=".aura.ui.AurakaiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/AuraKai"
        android:supportsRtl="true"
        android:theme="@style/Theme.dev.aurakai.auraframefx"
        tools:targetApi="36">

        <!-- Xposed Module Metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />


        <!-- YukiHook API Metadata -->
        <meta-data
            android:name="yukihookapi_module"
            android:value="true" />
        <meta-data
            android:name="yukihookapi_module_name"
            android:value="Genesis-OS AI Framework" />
        <meta-data
            android:name="yukihookapi_module_version"
            android:value="1.0.0" />

        <!-- App Startup Initializer -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="dev.aurakai.auraframefx.core.initialization.AppInitializerInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- Main Activity -->
        <activity
            android:name="dev.aurakai.auraframefx.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Services -->
        <service
            android:name="dev.aurakai.auraframefx.app.VertexSyncService"
            android:exported="false" />
        <service
            android:name="dev.aurakai.auraframefx.security.IntegrityMonitorService"
            android:exported="false" />
        <service
            android:name="dev.aurakai.auraframefx.services.BackupService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name="dev.aurakai.auraframefx.oracledrive.genesis.ai.GenesisBackendService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <service android:name="dev.aurakai.auraframefx.services.AmbientMusicService" />

        <!-- Receivers -->
        <receiver
            android:name="dev.aurakai.auraframefx.receivers.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Integrity Violation Receiver -->
        <receiver
            android:name="dev.aurakai.auraframefx.security.IntegrityViolationReceiver"
            android:exported="false"
            android:permission="dev.aurakai.auraframefx.permission.RECEIVE_INTEGRITY_VIOLATION">
            <intent-filter>
                <action android:name="dev.aurakai.auraframefx.action.INTEGRITY_VIOLATION" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
